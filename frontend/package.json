{
  "private": true,
  "workspaces": {
    "packages": [
      "provenance-core",
      "provenance-tree-visualization-grouping",
      "slide-deck-visualization",
      "ami",
      "storyvis"
    ],
    "nohoist": [
      "**/@angular*",
      "**/@angular*/**",
      "**/@ngtools*",
      "**/@ngtools*/**"
    ]
  },
  "scripts": {
    "build:libs": "cd provenance-core && npm run build && cd ../provenance-tree-visualization-grouping && npm run build && cd ../slide-deck-visualization && npm run build",
    "postinstall": "npm run build:libs"
  },
  "dependencies": {
    "@angular/animations": "^10.1.5",
    "@angular/cdk": "^10.2.4",
    "@angular/core": "^10.1.5",
    "@angular/material": "^10.2.4",
    "tslib": "^2.0.3",
    "typings": "^2.1.1"
  },
  "devDependencies": {
    "@angular/cli": "^10.1.6",
    "typescript": "~4.0.3"
  }
}
